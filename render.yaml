services:
  - type: web
    name: testgen-ai
    runtime: python
    rootDir: project
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
